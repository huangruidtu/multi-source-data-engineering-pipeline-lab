# Module 07 — Interview talking points

**30 seconds:** “I treated validation as evidence, not a green label. The matrix distinguishes `BLOCKED` from `NOT_RUN`, and a pass needs run-specific evidence.” 中文：这是可靠性思维，不是文档美化。

**Failure story:** “I found and corrected a false-PASSED runner path by making exit-code and evidence semantics explicit. That matters because a failed command must never upgrade the project’s claim.” 中文：强调事实准确性。

**Production improvement:** “I would add scoped SLIs/SLOs, lag/checkpoint/quality alerts, secret/IAM governance, evidence retention and DR procedures after the current runtime baseline exists.” 中文：明确这是下一步，不是已实现。
